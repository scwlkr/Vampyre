# Endpoints

No Vampyre-owned HTTP endpoints are implemented.

External endpoints used by internal clients:

- GitHub REST API through `src/github/client.ts`.
- Telegram Bot API through Telegram command and ping modules.

These are implementation dependencies, not public Vampyre endpoints.
