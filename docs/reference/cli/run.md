# `run`

There is no generic top-level `vampyre run` command.

Implemented run surfaces:

- [`daemon run`](./daemon.md) for the foreground daemon loop.
- [`agent run`](./agent-run.md) for the Worktree Build Agent.
- [`validation request`](./validation-request.md) for native validation.
