# `--help`

Prints the implemented command surface.

## Usage

```sh
node dist/cli.js --help
node dist/cli.js -h
```

The help output is implemented in `src/cli.ts` and should be checked after
adding or changing command-line flags.
