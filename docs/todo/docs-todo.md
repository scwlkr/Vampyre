# Docs Todo

## Current

- Keep `docs/STATUS.md` short after each implementation session.
- Keep command reference pages in sync with `node dist/cli.js --help`.
- Add examples only after the command has current proof.

## Potential Follow-up

- Add a generated docs link checker if docs churn continues.
- Decide whether to keep both `docs/to-do/` and `docs/todo/` long term.
- Add CI to run TypeScript, tests, build, markdown link checks, and
  `git diff --check`.
